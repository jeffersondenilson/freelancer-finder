<div>
  <h1>Meus projetos</h1>
  <% if current_user.projects.empty? %>
    <p>Você ainda não tem projetos</p>
    <%= link_to 'Crie um projeto', new_project_path %>
  <% else %>
    <%= render current_user.projects %>
  <% end %>
</div>
