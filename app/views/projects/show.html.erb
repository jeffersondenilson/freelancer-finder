<% if user_signed_in? %>
  <%= link_to 'Editar', edit_project_path(@project) %>
<% elsif professional_signed_in? %>
  <%= link_to 'Fazer proposta', new_project_proposal_path(@project) %>
<% end %>
<br>
<%= render @project %>
<br>
<% if professional_signed_in? && @proposal %>
  <h2>Sua proposta</h2>
  <%= render @proposal %>
<% end %>
